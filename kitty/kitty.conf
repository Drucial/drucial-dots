# Import Theme
include rose-pine-moon.conf

# Font Rendering
font_family      JetBrainsMono Nerd Font Mono
bold_font        auto
italic_font      auto
bold_italic_font auto
font_size        14
cursor_shape     block
cursor_trail     1
cursor_trail_decay 0.1 0.25
cursor_beam_thickness 2.5
shell_integration no-cursor


# Window Appearance
background_opacity      0.80
background_blur         100
window_margin_width     0
window_padding_width    8 8
hide_window_decorations titlebar-only
# remember_window_size  no
# placement_strategy center

# Tab Appearance
tab_bar_margin_width    8
tab_bar_margin_height   8 0
tab_bar_style           powerline
tab_powerline_style     slanted

# Window Behavior
window_border_width 1pt
draw_minimal_borders true
focus_follows_mouse no
macos_quit_when_last_window_closed yes
macos_show_window_title_in none
scrollback_lines 10000
action_alias launch_tab launch --type=tab --cwd=current

enabled_layouts tall,fat,grid,stack,horizontal,vertical

# Possible perfomance hit: https://sw.kovidgoyal.net/kitty/conf/#performance-tuning
repaint_delay 8

# Keybindings
map cmd+\ new_window_with_cwd
map cmd+shift+\ launch --location=hsplit --cwd=current
map cmd+t new_tab_with_cwd

# Optional: arrow-key versions
map alt+left  resize_window narrower 5
map alt+right resize_window wider   5
map alt+up    resize_window taller  3
map alt+down  resize_window shorter 3

# Reset all pane sizes in the tab
map cmd+alt+0 resize_window reset

# Replace current layout with 70/30 vertical split
map cmd+shift+d combine : goto_layout splits : launch --location=vsplit --bias=30 --cwd=current

# Toggle to stack layout (fullscreen) - press again to return to previous layout
map cmd+shift+f toggle_layout stack

# Reload kitty configuration
map cmd+shift+r load_config_file

# Search scrollback buffer
map cmd+f launch --type=overlay --stdin-source=@screen_scrollback --stdin-add-formatting fzf --ansi --exact --style=full --color=16 --height=100% --margin=1,2 --preview='echo {}' --preview-window='right:50%:hidden:wrap' --bind='ctrl-/:toggle-preview'


map ctrl+j kitten pass_keys.py bottom ctrl+j
map ctrl+k kitten pass_keys.py top    ctrl+k
map ctrl+h kitten pass_keys.py left   ctrl+h
map ctrl+l kitten pass_keys.py right  ctrl+l

# Arrow key navigation between splits
map cmd+shift+down  kitten pass_keys.py bottom cmd+shift+down
map cmd+shift+up    kitten pass_keys.py top    cmd+shift+up
map cmd+shift+left  kitten pass_keys.py left   cmd+shift+left
map cmd+shift+right kitten pass_keys.py right  cmd+shift+right

allow_remote_control yes
listen_on unix:/tmp/mykitty
